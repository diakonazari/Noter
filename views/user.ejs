<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/styles.css" rel="stylesheet">
    <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
    crossorigin="anonymous"
  />
    <title>NOTER</title>
</head>

<body style="background-color:#aabbff;" >
    <header style="display: flex ; flex-direction: column; align-items: center" >
        <%- include('partials/navbar',{activePage : 'user'}) %>
        <h1> Welcome <%= user.firstName %></h1>
    </header>
    <% if (user.notes.length !== 0) { %>
    <main style="display: inline-flex;">
            <%- include('partials/sidebar') %>
        <% } else { %>
          <main style="display: flex; justify-content: center;">
         <% } %>
       <form action="<%= foundNote._id ? ('/api/noting?titleId='+foundNote._id) : '/api/noting' %>" method="POST" class="notes-form">
        <div class="mb-3">
            <label for="exampleFormControlInput1" class="form-label">Title</label>
            <input type="text" name="title" class="form-control" id="exampleFormControlInput1" value="<%= foundNote.title || '' %>" />
          </div>
          <div class="mb-3">
            <label for="exampleFormControlTextarea1" class="form-label">Note</label>
            <textarea name="note" type="text" class="form-control" id="exampleFormControlTextarea1" rows="20" ><%= foundNote.note || '' %></textarea>
          </div>
          <div class="col-12">
            <button class="btn btn-primary" type="submit">Save</button>
          </div>
       </form>
    </main>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
      crossorigin="anonymous"
    ></script>
</body>
</html>